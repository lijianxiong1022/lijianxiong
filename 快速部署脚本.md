# 🚀 快速部署脚本使用指南

## 服务器端一键部署

### 1. 上传文件到服务器
```bash
# 使用SCP上传整个项目
scp -r /path/to/test1 root@your_server_ip:/var/www/order-system
```

### 2. 在服务器上执行
```bash
# 连接服务器
ssh root@your_server_ip

# 进入项目目录
cd /var/www/order-system/backend

# 创建生产环境配置
cp .env.production.example .env.production
nano .env.production  # 编辑配置

# 运行部署脚本
chmod +x deploy.sh
./deploy.sh
```

### 3. 配置Systemd服务
```bash
# 复制服务文件
sudo cp backend/systemd/order-system.service /etc/systemd/system/

# 修改服务文件中的路径（如果需要）
sudo nano /etc/systemd/system/order-system.service

# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable order-system
sudo systemctl start order-system
```

### 4. 配置Nginx（可选）
```bash
# 复制Nginx配置
sudo cp nginx/order-system.conf /etc/nginx/conf.d/

# 编辑配置（修改域名/IP）
sudo nano /etc/nginx/conf.d/order-system.conf

# 测试并重启
sudo nginx -t
sudo systemctl restart nginx
```

---

## 本地开发环境配置

### 保持本地配置不变
本地 `backend/.env` 文件：
```env
USE_SQLITE=True
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
```

前端会自动检测环境，本地开发时使用 `localhost:5000`，部署后使用服务器地址。

---

## 更新部署流程

### 方法1：使用Git（推荐）
```bash
# 本地
git add .
git commit -m "更新说明"
git push

# 服务器
cd /var/www/order-system
git pull
cd backend
./deploy.sh
```

### 方法2：手动上传
```bash
# 本地打包（排除不需要的文件）
# 上传到服务器后
cd /var/www/order-system/backend
./deploy.sh
```

---

## 检查清单

部署前确认：
- [ ] MySQL数据库已创建
- [ ] 数据库用户已创建并授权
- [ ] `.env.production` 已配置
- [ ] Python 3已安装
- [ ] 依赖已安装
- [ ] 服务已启动
- [ ] 端口已开放

部署后测试：
- [ ] 访问 http://your_server_ip 查看前端
- [ ] 访问 http://your_server_ip/api/v1/user/register 测试API
- [ ] 测试登录功能
- [ ] 测试报单功能


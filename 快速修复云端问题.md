# 快速修复云端问题

## 🚀 一键排查和修复

### 方法1：使用排查脚本

```bash
# 运行排查脚本（需要先上传到服务器或本地执行）
bash 云端问题排查脚本.sh
```

### 方法2：手动快速修复流程

```bash
# 1. 检查服务状态
ssh root@175.24.47.45 "systemctl status order-system"

# 2. 查看错误日志
ssh root@175.24.47.45 "journalctl -u order-system -n 50 --no-pager | tail -20"

# 3. 如果代码问题，本地修复后推送
git add .
git commit -m "修复说明"
git push origin master

# 4. 服务器更新
ssh root@175.24.47.45 "cd /var/www/order-system && git pull origin master && systemctl restart order-system"

# 5. 验证修复
curl http://175.24.47.45/api/v1/user/register
```

---

## 🔍 常见问题快速修复

### 问题：API不工作

```bash
# 1. 重启服务
ssh root@175.24.47.45 "systemctl restart order-system"

# 2. 检查日志
ssh root@175.24.47.45 "journalctl -u order-system -n 20"

# 3. 检查端口
ssh root@175.24.47.45 "netstat -tlnp | grep 5000"
```

### 问题：前端功能异常

```bash
# 1. 对比文件
bash 对比本地和服务器代码.sh

# 2. 如果不同，重新上传
scp all_pages.html root@175.24.47.45:/var/www/order-system/

# 3. 清除浏览器缓存（用户操作：Ctrl+F5）
```

### 问题：数据库连接失败

```bash
# 1. 检查数据库服务
ssh root@175.24.47.45 "systemctl status mysql"

# 2. 测试连接
ssh root@175.24.47.45 "mysql -u order_user -pljx19921022.. order_system -e 'SELECT 1;'"

# 3. 检查配置
ssh root@175.24.47.45 "cat /var/www/order-system/backend/.env.production"
```

---

## 📋 标准排查流程

1. **查看日志** → 找出错误信息
2. **检查服务** → 确认服务运行
3. **对比代码** → 确认版本一致
4. **测试API** → 确认接口可用
5. **修复问题** → 本地修复后推送
6. **更新服务器** → 拉取最新代码
7. **验证修复** → 测试功能正常

---

## 💡 预防措施

1. **本地测试充分**：确保本地功能正常
2. **版本控制**：所有修改都提交Git
3. **定期同步**：保持本地和服务器一致
4. **监控日志**：定期查看服务器日志
5. **备份数据**：重要修改前备份

